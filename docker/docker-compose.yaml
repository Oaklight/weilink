services:
  weilink:
    image: oaklight/weilink
    ports:
      - "45678:8000"  # MCP SSE
      - "49999:8080"  # Admin panel
    volumes:
      - weilink-data:/data/weilink
    restart: unless-stopped

volumes:
  weilink-data:
